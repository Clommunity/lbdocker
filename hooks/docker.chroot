#!/bin/sh

apt-get -y update
apt-get install -y -q lxc-docker

# Impoort Conatiner
# 
# docker import http://repo.clommunity-project.eu/images/unstable/cloudy.container.tar.gz

# Forcer umount system!
umount cgroup
umount cgroup
umount cgroup
umount cgroup
umount cgroup
umount cgroup
umount cgroup
umount cgroup
umount cgroup


# Fer canvis de fitxers:

rm /etc/default/docker
mv /etc/default/docker.changer /etc/default/docker

exit 0

